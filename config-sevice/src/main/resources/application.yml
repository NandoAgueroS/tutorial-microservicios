#spring.application.name=config-sevice
server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/NandoAgueroS/tutorial-microservicios
          search-paths: config-data